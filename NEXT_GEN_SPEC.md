# 新一代「电源柜调试记录」需求与技术规范（复刻 + 增强）

> 本文档是**唯一需求源**，目标是：把它交给任何一个"全新对话窗口的 AI"，**不需要任何补充说明、不需要翻阅仓库其它文件**，就能重建出与现版功能一致、且更稳健的新版安卓 App。
>
> 现版 App =「电源柜调试记录」v2.25（versionCode 28，DB schema=10，备份 schemaVersion=9）。新版不是从零发明，而是**复刻现版全部能力 + 按第 7 章改造多端同步架构 + 修复第 11 章全部已知缺陷**。

---

## 1. 总览与核心理念

面向**电力现场**（馈电柜 / 直流屏等柜子调试）的调试日志安卓应用。数据存储在手机本地 SQLite（Room）；**仅在用户主动配置并使用 WebDAV 时才访问用户自己指定的服务器**；不集成任何第三方 SDK、无统计、无遥测。

核心理念：
- 数据先在本地完整记录，网络只是"团队互通"的搬运手段，任何时候断网都能全功能使用。
- 有据可查：每一次"通过/故障/消除"都落成独立日志条目（时间、人员、内容），形成可回溯时间线，**永不合并展示**。
- 删除必须谨慎且可传播：删除操作在全体设备间收敛，不允许"这边删了那边长回来"，但也不允许误删扩散。
- 状态在根子上算对：测试项三态与故障的推演收敛到**单一推导入口 + 幂等自愈**，而不是事后到处补丁。

---

## 2. 铁律（任何实现都不得违反）

1. **签名永久一致**：**v2.26 起为新软件、新仓库、新签名**（包名 `com.powerdebug.record`，与老项目 power-cabinet-debug-log/≤v2.25 用户正式分家；老包用户需卸载重装，签名链在 v2.26 定点重置）。本线所有历史/未来版本靠**同一 keystore**（alias=powerdebug）签名保证覆盖安装。发布必须验签（SHA-256 指纹 `71651EC50784C17E17758EE234373C19095D4BD899ED931174DD2DFD352178F8` 恒定）。禁止替换/删除 keystore、禁止改 alias 或密码。实名密钥材料（加密压缩包 `app/signing/powerdebug.keystore.zip`）入库，**解压密码永不写入代码/README/提交信息**，由项目所有者掌握；CI 用 Secrets（SIGNING_ZIP_PASSWORD / SIGNING_STORE_PASSWORD / SIGNING_KEY_PASSWORD）自动解压+签名。
2. **绝不 fallbackToDestructiveMigration**：Room 迁移链必须完整、纯 SQL、可重复，任何版本升级不得丢库。
3. **日志"测试人员"绝不回落登录账号**：日志与测试结果的人员归属取"当前调试员"；删除/改名调试员不影响历史日志。
4. **故障已解决不自动过关，必须人工复测**：测试项关联故障被标"已解决"后，该项保持"未通过/待测"，只有测试员点"通过"才转绿（复测✓自动把仍待解决的关联故障标为已解决）。
5. **UI 文案全部走 strings.xml**，不硬编码中文；代码中文注释是仓库惯例。
6. **全程禁止明文密码入库/上传**；WebDAV 口令只在内存与系统偏好里短期使用。
7. **诊断优先**：一切同步、崩溃过程必须落本地诊断日志（SyncLog + CrashLog），作为现场排查的标准入口。
8. **版本节奏**：功能完成 → versionName/versionCode 递增 → README 变更说明 → push → CI 绿 → APK 放项目根 →（重要版本）GitHub Release。
9. **新增表/字段的三处同步**：DB version+1 写纯 SQL 迁移 + 备份 schema+1 + parseBackup/restoreJson/merge 三处同步更新 + README/规格文档记录变更说明。

---

## 3. 数据模型（Room，库名 power_debug.db）

实体清单：Project, CabinetType, CandidateItem, CabinetInstance, DebugLog, FaultRecord, PlannedItem, TesterAccount, Debugger, DeletedItem。

**外键级联**：删除父行 → 级联删子行（下一层全清）+ 写删除墓碑。

### 3.1 projects（项目）
- id TEXT PK（UUID）；name 必填；code、remark；createdAt、updatedAt（毫秒）。

### 3.2 cabinet_types（柜子类型 = 模板）
- id PK；name、remark；createdAt、updatedAt。
- 每种类型维护一份**候选池** candidate_items。

### 3.3 candidate_items（候选池 = 类型常用测试项清单）
- id PK；typeId → cabinet_types（ON DELETE CASCADE）；content 文本。
- 唯一索引 `(typeId, content)`。
- 语义：保存日志时，测试内容中不存在的行自动沉淀（自动去重）；用于"建柜自动生成预选清单"与手动补充。

### 3.4 instances（柜子实例 = 现场实际设备）
- id PK；projectId → projects（CASCADE）；typeId → cabinet_types（CASCADE）。
- name 必填；deviceCode、location、installer 选填；shortName（精简名，网格视图用）；sortOrder（拖动排序，0=按名称）；rowGroup（行分组，0=未分组）；createdAt、updatedAt。

### 3.5 debug_logs（调试日志条目）
- id PK；instanceId → instances（CASCADE）；circuit（回路号，空=整柜）；testContent（测试内容，多行）；tester（测试人员）；remark；createdBy / updatedBy（账号）；createdAt / updatedAt。
- **logType**：`0=通过 PASS`、`1=故障 FAULT`、`2=消除 RESOLUTION`。一条日志只属一种类型；三类日志由"开始测试/故障列表"独立生成，手动写日志默认 logType=0。
- 语义：**故障日志的 remark=故障现象；消除日志的 remark=被解决故障的现象（用于匹配）**。

### 3.6 fault_records（故障记录）
- id PK；logId → debug_logs（CASCADE；指向该故障的**故障日志**）；circuit；symptom（现象）；solution（解决方法）；occurredAt；resolvedAt（0=未解决）；status：`0=待处理 PENDING`、`1=已解决 RESOLVED`；updatedAt。

### 3.7 planned_items（预选待测 = 柜子测试清单）
- id PK；instanceId → instances（CASCADE）；content（测试项描述）；enabled（启用）；doneAt（最近一次测试时间，0=从未测过）；logId（指向最近一次**通过日志**，空=无）；result：`0=未测`、`1=通过 RESULT_PASS`、`2=未通过 RESULT_FAIL`；faultId（未通过/驳回时关联的故障 id 列表，逗号分隔）；createdAt、updatedAt。
- 唯一索引 `(instanceId, content)`。

### 3.8 tester_accounts（测试员账号，本地）
- id PK；username 唯一；source；createdAt。

### 3.9 debuggers（调试员名单，与账号无关）
- id PK；name 唯一；createdAt、updatedAt。增/改/删全部要超级口令。

### 3.10 deleted_items（删除墓碑）
- id PK；tbl（表常量：projects/types/cands/instances/logs/faults/planned/debuggers）；itemId（被删行 id）；deletedAt；唯一索引 `(tbl, itemId)`。
- 语义：**所有显式删除入口**写入墓碑；快照携带全量墓碑；合并端先应用墓碑再决死/存活判定（见 7.3/7.4）。

---

## 4. 功能清单（现版全部能力，缺一不可）

### 4.1 项目 / 类型 / 柜子
- 项目增删改；类型（模板）增删改，每类型维护候选池（单条删除、批量粘贴导入、自动沉淀、按使用频次排序）。
- 柜子：建柜时自动从类型候选池复制生成预选测试清单；列表网格/列表切换、精简名称模式、自定义拖动排序、分组。
- 单击柜子直接进"开始调试"（无清单先跳预选待测页）；长按菜单：编辑 / 预选待测 / 新建日志 / 导出本柜 / 删除。
- 项目卡/柜子行实时显示 待测·未通过·待处理故障 三个数字。

### 4.2 预选待测管理页
- 清单行：开关启用、状态（通过时间 / 未通过+原因 / 待测）、删除单条。
- 顶部统计：总数 / 待测 / 未通过 / 通过 / 停用。
- 手动添加自定义项（多行自动按行拆、去重）；输入框下实时联想候选池"包含输入文字"的相似项（标注使用次数 / "已在本柜"），点选回填标准描述。
- **候选手选器**：候选项按**使用频次降序**（该类型全部柜子清单中的出现次数），长按拖动连续多选、"常用选取"（使用≥2次一键勾选）、全选、清空。
- 跨柜拉取：柜子长按"从别的柜子拉取预选待测"→ 全库柜子列表可按项目名/柜子名/设备编号搜索 → 用来源柜启用清单**整体覆盖**本柜清单（旧项删除并记墓碑）。
- 改为常用模板：项目卡长按"加入常用模板"=把项目下各柜启用待办项沉淀进各自类型候选池（已有同名自动跳过）。

### 4.3 手动写日志（保存日志）
- 两级定位（项目→柜子）后自动加载该类型候选池，勾选一键填充测试内容（可手改）。
- 回路号自由输入 + 历史回路联想下拉；留空=整柜测试。
- 保存时可编辑挂接故障（回路/现象/解决方法/发生时间/解决完成时间/状态）。
- **预选待测联动**：保存时把内容中命中本柜未完成项（宽容匹配：忽略行尾标点）标为"通过"并挂到本日志；命中项上次未通过 → 其关联故障随本次通过自动解决。
- 自动沉淀候选池。
- 编辑过显示"·已改"标记与创建/修改双时间+账号。

### 4.4 开始测试页（现场模式）
- 待办区 = 启用且 result≠通过 的项（未通过排前）；下方只读展示已通过项（绿色✓+完成时间）。
- 每项两个按钮：通过（✓） / 问题（✗）。**所有操作先暂存内存，点击"生成日志"统一落库**：
  - 点✗ → 弹窗强制填故障现象（多行=多条故障）。
  - 点✓ → 检查该项未解决故障数：有 → 弹"有 N 条未解决故障，确认通过？"；无 → 直接列入通过。
  - 已通过项点文字 → 时间线；长按 → 驳回菜单（见 5.5）。
- 生成日志 = 逐条独立生成（**永不合并**）：每条故障 = 一条故障日志 + 一条故障记录；每条消除 = 一条消除日志（故障标已解决）；每条通过 = 一条通过日志（挂 PlannedItem.logId）。同会话故障+消除+通过在同一事务内顺序生成（先故障后消除最后通过），幂等可重跑。
- 顶部显示柜名/设备编号（空=整柜）/当前调试员（点击可切换）。
- 返回有未保存操作时弹"保存并生成 / 放弃修改 / 取消"。

### 4.5 故障列表
- 列出该项故障：现象 + 已解决（删除线+绿"已处理"+"方法：xxx"）/ 待处理。
- 每条可"通过"（确认消除）/ "撤回"（恢复待处理，可撤回已解决项重新开测）。
- 点"通过"弹窗可填**解决方法**（可留空，同时保存到 FaultRecord.solution）。
- **最后一个故障确认消除后** → 自动把该项标通过并生成通过日志（即使没点清单里的"通过"）；测试列表只有一个未解决故障时点通过会同时生成消除+通过两条日志。
- 时间线消除条目直接显示"方法：xxx"，点击可随时编辑/补充，测试页与日志页读同一 DB 同步。

### 4.6 时间线
- 按 (instanceId, testContent) 汇总 debug_logs，按时间排序，渲染 通过/故障/消除 三类条目；头部统计总数与三类计数。
- 故障条目点击 → 故障详情；消除条目点击 → 编辑解决方法。
- **备注：时间线上重复的"通过"是真实独立操作（各自时间戳），属审计链的一部分，不做合并/去重。**（用户已明确不需要合并展示）

### 4.7 调试员与账号
- **调试员 debuggers**：与登录账号无关的现场人员名册，增/改/删均需超级口令；本机"当前调试员"存本机偏好 SyncStore.currentDebugger；写日志/开始测试自动归属，点击可切换；改名/删除不动历史日志；随备份与 WebDAV 互通全队。**日志测试人员绝不回落登录账号**。
- **测试员账号 tester_accounts**：登录验证=WebDAV 连通性（PROPFIND + Basic），无独立账号体系；登录弹窗内置"测试连接"按钮，失败时显示完整诊断报告（HTTP 状态码/响应头/排查提示，可复制）。
- **超级口令 `mz9890517`**：在登录框密码栏输入即可**离线**注册为本地测试员（属产品功能而非机密，源码内可写）。

### 4.8 查询与筛选
- 项目 / 类型 / 实例三级联动下拉 + 故障状态筛选（全部 / 待处理 / 已全解决）+ 回路号筛选 + 全文搜索（测试内容、备注、测试人、故障现象、解决方法、柜名、项目名）。

### 4.9 数据工具
- 导出全部日志为标准 **.xlsx**（两个工作表：调试日志、故障记录；A4 打印适配——一页宽缩放、每页重复表头、冻结首行、列宽自适应；故障表横向打印；日志表含创建/修改账号列）。定向导出：项目卡长按"导出本项目日志"、柜子长按"导出本柜日志"，同格式。
- JSON 备份 / 恢复（恢复前强提示覆盖；兼容旧 schema 自动迁移）。
- **从备份找回被删记录**（见 7.6）：扫描备份中本机缺失的行权威插回、刷新时间戳、清墓碑，经同步传播回全队。
- **修复日志类型**（见 7.7）：把被故障记录指向却 logType=0 的日志重分类为"故障"，把备注=已解决故障现象、同柜同内容的日志重分类为"消除"；配「撤销类型修复」。
- 查看同步日志 / 崩溃日志（复制 / 清空）。

---

## 5. 核心状态机（测试项 × 故障 的推演规则）

**原则：提前在根子上把状态算对，而不是事后到处补 bug——这是新版必须优于旧版的核心。**

### 5.1 测试项三态 result
- 未测 0 / 通过 1 / 未通过 2。PASS 时 logId 指向最新通过日志，doneAt=通过时间。
- 未通过/驳回时 faultId 保存关联的未解决故障 id（逗号分隔）。

### 5.2 状态推导（唯一权威入口 recomputePlannedState(instanceId, content)）
任何"删除日志 / 生成日志 / 撤销 / 合并 / 恢复 / 修复"之后，对该 (instanceId, content) 重算：
1. 查询该项当前**未解决故障**（status=0 且其故障日志归属该柜该内容）。
2. 若存在未解决故障：
   - 该项**不应有通过日志**——若此前是 PASS 且 logId 有效，删除该通过日志。
   - 设 result=未测、logId 清空、faultId=未解决故障 id 列表。（历史 bug：曾用"故障日志 id"去 resetForLog 而 PlannedItem.logId 存的是"通过日志 id"，从不命中导致状态残留——**必须按 instanceId+content 重算，禁止按错误 id 匹配**）
3. 若无未解决故障：
   - 有有效通过日志（logId 指向现存 logType=PASS 日志）或已是通过态 → 保持"测试通过"并清空失效 faultId。
   - 否则 → 回退"待测"（无故障且从未通过不能凭空标通过）。

### 5.3 幽灵自愈 healGhostFailures（幂等）
- 定义：某测试项 faultId 指向的故障记录**已全部不存在**（来源：删除日志级联、旧快照合并、历史残留）→ 界面显示"原因见日志"但故障列表为空，即"幽灵状态"。
- 规则：**此类项一律自动纠正为「测试通过」**（悬空=实际无故障）。仅处理"完全悬空"；只要有任意一条故障记录仍存在（含已解决待复测）就不动，守住铁律 4。
- 触发时机：应用启动、删除日志后、打开开始测试页、打开预选待测页。
- 要求：并入 5.2 的统一推导，做成幂等自愈（每次加载前执行，无副作用时零写入）。

### 5.4 保存日志命中联动
- 见 4.3：命中未完成项 → PASS + 挂日志 + 自动解决关联故障。

### 5.5 驳回已通过项 rejectPassedItem
- 长按已通过项 → 弹窗填故障现象（必填）→ 驳回：删除该通过日志（记墓碑）、删除该日志下关联故障（记墓碑）、设 result=未测、logId/faultId 清空，新故障现象带进当前会话（走 4.4 暂存流）。

### 5.6 复测转绿
- 未通过项在清单里点"通过"：先自动解决其未解决故障（resolveByIds），再生成消除+通过日志、置 PASS、挂 logId。

---

## 6. 删除语义（deleteLog + 级联 + 墓碑）

按日志类型弹删除方式，**全部显式删除入口都要 markDeleted 写墓碑**：
- **通过日志**：restore 为未测(RESTORE_PLANNED) 或 连预选项一并删除(PURGE_PLANNED，被删预选项各自记墓碑)。
- **故障日志（DELETE_FAULT）**：删该故障日志 + 该故障的消除日志 + 该 FaultRecord → 对该 (instanceId, content) 执行 5.2 重算 → 记日志/故障墓碑。
- **消除日志（DELETE_RESOLUTION）**：删消除日志 + 对应故障恢复为待处理(unpassSingle) → 重算 → 记日志墓碑。
- **消除+故障一起删（DELETE_RESOLUTION_PURGE）**：删消除日志 + 故障日志 + 故障记录 → 重算 → 记日志/故障墓碑。
- 删除项目/类型/柜子/预选项同理走 DAO 删除；外键级联保证子行全删；**每行删除都记墓碑**。
- 铁律：**内部状态纠正（如 5.2 自动删通过日志）产生的删除，不得等同于"用户显式删除"永久扩散**——要区分语义并给出撤销路径。

---

## 7. 多端同步（★ 新版核心变更章）

> **用户只提需求**，两条硬性变更 + 一条兼容要求；具体实施细节由实现方 AI 决定（第 7 章后半的目录结构、阶段划分、迁移策略为**推荐方案**，可采纳可优化，但必须满足 7.0 的三条硬需求）。

### 7.0 新版同步的三条硬需求（用户明确要求，不得违背）
1. **云端全局文件夹**：不属于任何单个项目的全局数据——柜子类型(cabinet_types)、候选池(candidate_items)、调试员名单(debuggers)、测试员账号(tester_accounts)——统一存放在云端**全局区**，让所有项目/所有设备共用一份。
2. **按项目增量同步**：云端按"每个项目一个文件夹"组织；同步时**只观察本地有哪些项目/柜子有改动，只同步对应的数据**，不再上传/下发全量 JSON。
3. **兼容现版云端数据 + 独立文件夹**：新版必须在云端**新的文件夹**中同步（不占用现版 `backup_<账号>_<设备ID>.json` 所在目录），但必须**能读取并兼容**现版（v2.x）已产生的云端数据（gzip 快照，schemaVersion=9），完成过渡后旧数据/旧目录可废弃。

附带用户关注点（可直接采纳）：全量→增量可大幅省流量、提速；冲突范围从"全库"缩到"单个项目"；云盘目录直观。但必须先解决两个架构陷阱：**全局数据孤岛**（全局数据不属于任何项目，只按项目同步会丢）、**本地修改检测**（不能信任安卓文件 mtime）。

### 7.1 现版（被复刻的）同步原理
- 所有手机配置同一个 WebDAV 目录（支持 http 明文如内网 NAS；支持中文路径）。
- 每台手机上传全量快照 `backup_<账号>_<本机标识>.json`（本机标识=设备唯一随机 tag，同账号多机不互覆；旧格式 `backup_<账号>.json` 仍被读取合并）。
- 打开应用/手动按钮时：先上传自己快照 → 列出目录所有快照 → 逐个合并进本机（60 秒节流防抖）。
- 立即原因：保存日志后推送 + 打开应用双向同步（60 秒节流）；手动"立即双向同步"按钮；同步全程写 SyncLog；失败不崩溃、可重试。

### 7.2 现版快照格式（backupJson，schemaVersion=9 —— 新版必须读得懂）
- JSON 顶层含：app 标识、schemaVersion、exportedAt、projects、cabinetTypes、candidateItems、instances、logs、faults、plannedItems、debuggers、**deletedItems（全量删除墓碑）**。
- 字段名 = 数据库列名（见第 3 章）；快照上传前 **gzip 压缩**（中文压到 1/8~1/15）；读取按魔数 `1f 8b` 自动识别，明文旧格式照常合并。
- 兼容要求：解析宽容缺字段（optString/optLong 兜底）；README 附录中的 JSON 各表字段就是交换契约，任何平台可解析。

### 7.3 现版合并规则（applyMerge）
按表顺序（父表在前保证外键引用）：项目→类型→候选→柜子→日志→故障→预选→调试员。
- 同 id：local.updatedAt 新者胜（更新），不存在才插入。
- **墓碑先行**：先入库远端墓碑，再统一应用删除。
- **孤儿跳过**：父链已断的行不插入；已被墓碑标记的行永不复活。
- 候选池/预选项额外按 `(typeId, content)` / `(instanceId, content)` 去重；调试员按 name 去重。
- 每份快照合并计数（新增/更新/删除）写入 SyncLog 并回显。

### 7.4 现版墓碑"新者胜"（v2.21 落地，语义必须继承）
- 判定：一行在快照中存在时，**若墓碑.deletedAt < 行.updatedAt → 墓碑"被击败"**，视为不存在（保留/插入该行，并删除本机这条墓碑，使各端收敛回"存活"）。反之删除生效（含外键级联）。
- 实现口径：先落库远端墓碑，汇总"本机存活行 ∪ 快照携带行"每行最新 updatedAt；墓碑 deletedAt 早于某行最新更新 → 先自清理（避免误删胜出父行的级联）；留存墓碑按表经 DAO 删除。本机无该行时墓碑照常入库（防"删了又长回来"）。
- 合并/恢复/修复后都要对相关 (instanceId, content) 执行 5.2 推导与 5.3 自愈。

### 7.5 推荐：新版目录结构（在 7.0-3 的"云端新文件夹"根下）
```
<新版云端根目录，用户新建，旧版不读写这里>
├── global/
│   └── backup_<账号>_global.json.gz      # tester_accounts, debuggers, cabinet_types, candidate_items
├── projects/
│   └── project_<UUID>/
│       └── backup_<账号>_project.json.gz # 该项目内的 instances, logs, faults, planned_items
├── legacy/                               # 旧版全量快照的"暂存/迁移区"（见 7.8 兼容迁移）
│   └── backup_<账号>_<设备ID>.json.gz
└── deleted_projects/                     # 或并入 global/，项目级删除墓碑
```
- `global/` 一次拉取，全设备共用：调试员名单 / 账号 / 柜型与候选池都是**全局收敛**（不分项目）。
- `projects/project_<UUID>/` 每个项目一个文件夹；文件夹名即项目 UUID（旧版 backup snapshot 里的 schemaVersion=9 字段名不变）。
- 快照文件仍按账号+设备标识命名，延续"多机不互覆"。

### 7.6 推荐：三阶段增量同步流程
1. **阶段 A — 拉全局**：GET `global/backup_<账号>_global.json.gz`（不存在则跳过），按 7.3 规则合并（全局数据时间戳晚于本机时优先合并），并处理 deleted_projects 墓碑。
2. **阶段 B — 枚举项目**：PROPFIND `projects/`，拿子文件夹名（=项目 UUID 列表），对比本机项目计算"云端有本机无 / 本机有云端无 / 双方都有"三类。
3. **阶段 C — 按项目差异上下传**：
   - **本地修改检测不依赖文件 mtime**（安卓厂商写入时间差异大、不可信）。改为：本机为每个项目维护 `projectLastSyncTime`（本地偏好，Map<ProjectId, Long>）；
   - 本地任一项目下发生增删改（柜子/日志/故障/预选项的创建、更新、删除墓碑）**都刷新该项目的版本时钟**（取该项目所有行最大的 updatedAt 即可，无需专用计数器）；
   - 上传：`项目.max(updatedAt) > projectLastSyncTime[项目]` → 生成该项目 JSON（或按需增量子集），PUT 到 `projects/project_<UUID>/`；随后把 projectLastSyncTime 推进到本次上传的时刻。
   - 下载：GET 云端该项目文件，按 7.3/7.4 合并（同项目内"新者胜"）；孤儿跳过；合并后 5.2/5.3 收尾。
   - **项目级删除**：删除项目 = 在该项目文件夹写删除墓碑（写入 `deleted_projects`/global 区），各端拉全局时按"新者胜"决定项目文件夹是否消亡。
4. 同步全程写 SyncLog（包含阶段清单、每次合并统计），失败不崩溃、可重试。

### 7.7 推荐：时区/时钟偏差兜底
- "新者胜"依赖各端系统时钟，跨时区/校时偏差会误判。
- 上传快照时 JSON 头部强制写 `deviceTimeZoneOffset`（毫秒）。
- 拉取合并时比对双方时间戳：若差距超阈值（如 > 5 分钟，仅当条目冲突时）→ 弹窗让用户选「保留本地」或「覆盖云端」，不静默判胜。

### 7.8 推荐：与现版（v2.x）数据的兼容迁移
- 新版**首次同步**时检查旧版目录 `backup_<账号>_<设备ID>.json(.gz)`（旧文件在旧版根目录；新版先让用户在旧目录授权读一次，仅迁移用）：
  1. 用 7.2 格式解码（魔数识别 gzip）；
  2. 按 projectId 把全量拆解：projects/instances/logs/faults/planned_items → 各自 `projects/project_<UUID>/`；cabinet_types/candidate_items/debuggers/tester_accounts → `global/`；deletedItems → 分发到对应项目/全局墓碑；
  3. 逐项目 PUT 后，旧文件改名/删除或标记 `.migrated`，避免重复搬运；
  4. 迁移完成后新版只读写新目录（global/ + projects/）；旧版仍读写旧目录，互不干扰，直到全队升级完毕可废弃旧目录。
- **注意**：现版数据中大量历史日志存在 `logType=0` 的归类失真（见 7.9 修复工具）——新版首次启动应内置同一套"日志类型修复"逻辑引导用户执行一次，或直接把 `logType` 的"可选模式"设为 UI 层兼容展示。

### 7.9 现版两个数据工具（新版必须保留，且作为新数据质量的防线）

**A. 从备份找回被删记录（rollbackFromBackup）**
- 触发场景：删除墓碑已传播到每台已同步手机，历史故障/消除日志全端消失；Excel 可能仍含丢失前导出的数据，库里已无。
- 语义：用户选定任一备份（明文 JSON 或 gzip 快照，按魔数识别）→ 扫描"备份有、本机无"的行（父表→子表顺序，孤儿跳过，内容去重/重名冲突跳过）→ **权威插回**：找回行统一刷新 `updatedAt`=当前时刻，并删除本机对应墓碑。
- 为什么刷新时间戳：若保持旧 updatedAt（早于墓碑 deletedAt），下次合并该行墓碑照常判死，找回立刻被再清除；刷新后 7.4"新者胜"必然击败一切历史墓碑，找回得以存活并传播。
- 传播：仅一台手机执行即可；该机下次上传携带找回行（新时钟）且不再带对应墓碑 → 其他手机合并时被动击败自身墓碑并插入，全端收敛。
- 预览与确认：先只统计（预览与执行共用同一计算口径）；"无缺失记录"时弹窗展示**备份 vs 本机日志构成对比**（总/通过/故障/消除/故障记录 条数）——备份内故障/消除为 0 说明该备份生成于丢失之后，应改选更早快照重试。
- 找回后触发 5.2/5.3。

**B. 日志类型修复（reclassifyLogTypes + 撤销）**
- 背景：logType（0通过/1故障/2消除）是 v2.18 新增列，默认 0。更早版本产生的、或之后被旧格式快照/旧备份合并进来的日志全为 0 → 列表/筛选/导出里"故障/消除"看着像丢光，但**日志行与故障记录一条没少**（fault_records.logId 始终指向对应日志；"已解决"状态即消除证据）。
- 两条规则（按数据事实反推，幂等）：①**故障**：`logType != 1` 且被 ≥1 条故障记录（logId 指回）→ 改 1。②**消除**：`logType != 2`、未被故障记录指向、备注非空，且同 instanceId+testContent 存在"已解决且 symptom==备注"的故障 → 改 2。
- 只改 logType 一个字段 + 刷新 updatedAt；不虚构不存在的日志；预览→确认→执行。
- 传播：刷新时间戳后下次同步以"新者胜"传回全队。
- 撤销：修复时把 (日志id→原logType) 写入本机审计 `FixLogStore`（本地偏好，非业务数据、不随备份/同步传播）；「撤销类型修复」按留存还原并刷新时钟，仅对留存之后的修复生效。

---

## 8. 备份 / 恢复 / 导出
- JSON 备份 = 7.2 快照格式（schemaVersion=9，兼容读取 1..9）；恢复前强提示覆盖。
- 恢复/解析要容忍缺字段（optString/optLong 兜底）、旧版本缺列。
- Excel 导出格式见 4.9（零依赖自实现 OOXML，`core/XlsxWriter.kt`）。

## 9. 诊断日志（现场排查标准手段）
- **SyncLog.kt**：每一次同步过程（目录/文件清单、每份快照合并统计或失败原因、墓碑应用明细）追加到本机，工具页可查看/复制/清空。
- **CrashLog.kt**：全局未捕获异常黑匣子，App.onCreate 安装，崩溃栈落应用内文件，工具页可查看/复制。
- 一切"两台手机看不到彼此数据""闪退白屏""数据消失"类现场问题，第一步就是让用户复制这些日志。

---

## 10. 编码 / 构建 / 发布
- 技术栈：Kotlin + Room + 传统 View + Material 组件（浅色工业风）+ ViewBinding；minSdk 28，targetSdk 34。零依赖 xlsx 生成器。`org.json` 做备份序列化。
- 仓库即交付物：代码、CI、文档都在。**只在 GitHub Actions 构建**（本地无 gradle wrapper / Android SDK，禁止本地 assembleDebug）；产物名 `power-debug-debug-apk`。
- CI 必须 `clean`（旧 Gradle 缓存曾导致 APK 未重编、DB 迁移错乱）；push 到 main 自动触发。
- 签名与发布节奏见铁律 8；发版必验签（指纹见铁律 1）。
- **新版必须补（现版缺失）**：为核心状态流加**自动化回归测试**并在 CI 跑起来——saveLog 命中联动 / generateIndependentLogs / deleteLog 五模式 / recomputePlannedState / healGhostFailures / applyMerge 含墓碑"新者胜"冲突 / reclassifyLogTypes / rollbackFromBackup。现状是"只保证编译不保证行为"。

---

## 11. 已知缺陷清单（新版必须从架构上避免，否则不算达标）

| # | 旧版缺陷 | 新版要求 |
|---|---------|---------|
| 1 | 测试项状态散落多处（recompute / healGhost / 各删除分支各算各的），删日志/合并后不一致，出现"原因见日志但无故障" | 状态**单一推导入口**（5.2）+ 幂等自愈（5.3），任何写库路径结束都收敛到它 |
| 2 | 墓碑无条件永久删除 + 永不回收，一机旧删除可清掉全端较新数据 | 墓碑"新者胜"（7.4），被击败墓碑自清理，各端收敛 |
| 3 | 内部状态纠正当作用户删除，写墓碑全端扩散 | 区分"用户显式删除"与"自动级联纠正"；级联纠正不产生永久墓碑，或提供撤销/恢复入口 |
| 4 | 删除故障日志时误用"故障日志 id" resetForLog，而 PlannedItem.logId 存的是通过日志 id，静默无效 | 删除类操作一律走 5.2 的 instanceId+content 重算 |
| 5 | 无回归测试，CI 只编译不验证行为 | 第 10 章自动化测试必须覆盖 |
| 6 | 删"消除日志"与删"故障日志"两条路径曾有重复/冲突的自动删"通过日志"逻辑 | 收敛到统一推导，删与删之间无重复副作用 |
| 7 | 同会话"故障+消除+通过"生成曾有缺失/病态 | 批量生成逐条独立、事务内先故障后消除最后通过，幂等可重跑 |
| 8 | 全量同步方案：带宽浪费大、合并范围全库、冲突半径大、云盘目录不直观、全局数据无归属 | 按项目增量同步 + 全局文件夹（第 7 章硬需求） |

---

## 12. 版本沿革（现版能力时间线 = 新版必须保留的特性集）
- v2.0 前：五级数据模型（项目→类型+候选池→柜子→日志→故障）；旧版 int 主键（v2 迁移成 UUID）。
- v2.4：预选待测三态闭环（建柜自动生成清单、✓/✗测试、强制故障、复测转绿）；状态统计三分；修改时间痕迹。
- v2.5：调试员名单（超级口令维护）。
- v2.6：定向导出（项目/柜子级）；开始测试页已通过只读区；复测自动解决故障；单击进调试、长按出菜单。
- v2.8：多机不互覆（快照带设备标识）。
- v2.9：常用模板 / 候选手选器（使用频次、长按拖选、常用选取）/ 跨柜拉取 / 删除墓碑机制。
- v2.10：诊断日志（SyncLog+CrashLog）；快照 gzip+魔数兼容。
- v2.11：修复"GET 请求带空 body 被坚果云拒绝导致团队互通从未真正合并"。
- v2.12：手选器涂选重做；跨柜拉取异常落盘堆栈。
- v2.13：添加联想（候选池包含匹配+次数标注）；修复获取占位符错位崩溃。
- v2.18：流程重构——开始测试逐条独立生成日志；故障列表改造；时间线；删除逻辑分层（RESTORE/PURGE/DELETE_FAULT/DELETE_RESOLUTION/DELETE_RESOLUTION_PURGE）；DB v10 增加 logType。
- v2.19：故障处理方法全链路（通过弹窗填解决方法、时间线编辑方法、末个故障自动通过生成日志）；删日志后按事实重算测试项状态。
- v2.20：幽灵状态自愈（5.3）。
- v2.21：墓碑"新者胜"落地于合并阶段 0.1，被击败墓碑自清理。
- v2.22：从备份找回被删记录；解码魔数识别抽为公共函数复用。
- v2.23：找回扫描改为预览共用同一口径；"无缺失记录"时展示备份/本机日志构成对比。
- v2.24：日志类型修复（故障重分类）。
- v2.25：日志类型修复扩展为消除重分类 + 本机 FixLogStore 审计 +「撤销类型修复」。

---

## 附：验收清单（新版交付时逐项打勾）
- [ ] 数据模型字段与第 3 章一致；迁移链完整、非破坏
- [ ] 三态测试 + 故障/消除/通过三类日志独立生成 + 复测转绿
- [ ] 状态单一入口推导；幽灵自愈幂等
- [ ] 删除五模式 + 墓碑；墓碑"新者胜"且被击败自动回收
- [ ] **云端全局文件夹**：柜型/候选池/调试员/账号全局收敛（7.0-1）
- [ ] **按项目文件夹增量同步**：本地修改检测用项目版本时钟、不用文件 mtime；三阶段流程（7.6）
- [ ] **独立新文件夹 + 兼容旧数据**：能读现版 schemaVersion=9 gzip 快照并完成迁移（7.2/7.8）
- [ ] 从备份找回被删记录：预览构成对比→确认→权威插回+时钟刷新+清墓碑→同步传播
- [ ] 日志类型修复：故障+消除重分类（备注↔已解决故障现象匹配），幂等、预览确认、可撤销、同步传播
- [ ] 项目级删除墓碑（deleted_projects）同样"新者胜"
- [ ] 时区偏移上报 deviceTimeZoneOffset + 时间戳冲突弹窗（7.7）
- [ ] WebDAV 双向同步（gzip、多机不互覆、同步日志可查）
- [ ] 项目/柜子/类型的导出、全量导出、JSON 备份恢复
- [ ] 调试员归属绝不回落登录账号
- [ ] 文案全走 strings.xml；中文注释
- [ ] 签名一致可覆盖安装（新线 v2.26 起，SHA-256 指纹 `71651EC50784C17E17758EE234373C19095D4BD899ED931174DD2DFD352178F8`）；CI clean + 回归测试全绿